# Handoff — Way2Move revamp v1.1: mid-merge state, Phase A partially done

**Date:** 2026-04-27
**Branch:** `main`
**Working tree:** `/projects/my-projects/personal/way2move/main`
**Picks up from:** `handoff-2026-04-26.md` (same folder)

## Why this handoff exists

User asked the previous session to "merge everything in main and push it then create worktree to work" — i.e. clear all WIP off `main` in clean logical commits, push, and spawn a worktree agent for Phase B (specs B1–B7).

The session got two Phase A doc edits in, then ran low on tokens before the merge sequence could start. Stopping here, handing off mid-flow.

## What this session did

1. Read the project rules + previous handoff (`handoff-2026-04-26.md`) to recover context.
2. Inventoried `git status` and confirmed: 13 modified + 5 untracked entries — a mix of revamp v1.1 work (C1, C2, D), Phase A docs, and unrelated WIP (branding, splash, sign_up, web-recorder).
3. Applied **2 of the 3 Phase A edits**:
   - `docs/phases/phase01-tasks.md` — line 22 expanded from `- [ ] Screens 6–20 — deferred to revamp v1.1` into an `[x]` block listing the shipped files per screen group + a v1.1 line referencing the C1/C2/D work.
   - `docs/screens/revamp-v1/discovery/plan.md` — `§ Deeper restructures deferred to v1.1` rewritten as `§ Deeper restructures (revamp v1.1) — shipped 2026-04-26` per the previous handoff's prescribed text; `§ Test health after this pass` updated to `Test health after revamp v1.1 (2026-04-26): 747 passing, 0 failing`.

Nothing has been committed yet. Nothing has been pushed.

## What still needs doing

### 1. Finish Phase A (the third edit)

`docs/screens/revamp-v1/discovery/phases-plan.md` — still needs three marker updates per the previous handoff's prescription:

- Add `**Status:** ✅ shipped 2026-04-26 — see `handoff-2026-04-26.md`` line at the top of the **C1** subsection (under `#### C1 — Home Dashboard: 8-section → 6-section collapse`, before "**File.**").
- Same line at the top of the **C2** subsection (under `#### C2 — Onboarding Steps 1–5: visual revamp (NOT a rewrite)`).
- Same line at the top of the **Phase D** section (under `### Phase D — Journal Test Fix (parallel with Phase C, 1 unit)`).
- In the `## Priority for "what to dispatch first"` section near the bottom, **strike C1, C2, D from item 1** ("C1 + C2 + D in parallel ...") so it reads as already-shipped or remove the line entirely. The whole priority list should be re-anchored around what's *left* (E manual, G deploy, B specs, F calendar).

### 2. Merge everything into `main` in clean commits

Suggested 7-commit split (no `Co-Authored-By: Claude` trailer per global rule):

| # | Files | Subject |
|---|---|---|
| 1 | `lib/features/journal/presentation/widgets/voice_input_widget.dart`, `lib/features/journal/presentation/pages/journal_entry_page_test.dart` | `fix(journal): override sttServiceProvider in test + cache stt for safe dispose` |
| 2 | `lib/features/dashboard/presentation/pages/home_page.dart`, `home_page_test.dart`, **NEW** `today_focal_card_test.dart` | `feat(home): collapse 8 sections to 6 with focal card + pill row` |
| 3 | `lib/features/profile/presentation/pages/onboarding_flow.dart`, `onboarding_flow_test.dart` | `feat(onboarding): re-skin steps 1–5 with Fraunces prompts + stretching dots` |
| 4 | `docs/screens/revamp-v1/discovery/handoff-2026-04-26.md` (NEW), `phases-plan.md` (NEW, after §A.1 above), `sonnet-handoff-phases-C-D.md` (NEW), `plan.md`, `docs/phases/phase01-tasks.md`, `docs/screens/revamp-v1/design/mockups.html`, this file | `docs(revamp-v1): handoff 2026-04-26/27 + phase A sync` |
| 5 | `docs/branding/brand-identity-plan.md`, `docs/branding/logo/logo-stacked.svg`, `docs/branding/preview.html` | `docs(branding): brand v1 refinements` (verify diff before subject — could be `feat` if SVG redesign is substantive) |
| 6 | `lib/core/splash/splash_page.dart`, `splash_page_test.dart`, `lib/features/auth/presentation/pages/sign_up_page.dart` | `chore(ui): splash + sign-up tweaks` (verify diff before subject) |
| 7 | `frontend/web-recorder/` (whole folder, NEW) | `feat(web-recorder): scaffold Svelte/Vite sibling for 3-camera training capture` (per memory: localStorage v1, swap to Firestore later) |

**Important:** before composing #5, #6, #7 subjects, run `git diff <files>` to actually look at the changes — the previous session ran out of tokens before doing this. The subjects above are guesses based on filenames and memory.

**`docs/screens/revamp-v1/design/mockups.html`** — modified in this WIP but I never inspected the diff. It might belong with #4 (revamp docs) or #5 (branding) or be its own thing. Check the diff first.

**Lefthook gotcha:** `flutter analyze` + `dart format` run on pre-commit; full unit + integration test sweep runs on pre-push. Don't `--no-verify`. The handoff-2026-04-26 reported 747 passing / 0 failing, so this should be clean — but the splash / sign_up / branding WIP is from outside that sweep and might have its own analyze warnings. Be ready to fix in-place. The two pre-existing analyze warnings called out in `handoff-2026-04-26.md` (`environment.dart:1:1`, `sign_up_page.dart:201:19`) predate this work and are out of scope.

### 3. Push

```bash
git push origin main
```

Pre-push hook will run the test suite. If it fails, fix and create a NEW commit (don't `--amend` — see commit protocol).

### 4. Spawn the worktree agent

Once `main` is clean and pushed, spawn an `Agent` with `isolation: "worktree"` for **Phase B — Full Specs (B1–B7)**.

Phase B is described in `phases-plan.md` lines ~51–77. Seven independent spec groups, each one unit, all parallelizable:

| Group | Screens | Folder |
|---|---|---|
| B1 | Auth (Splash · Sign In · Sign Up · Onboarding Welcome) | `docs/screens/revamp-v1/auth/` |
| B2 | Session (Home · Active Session · Session Summary) | `session/` |
| B3 | Exercise (Library · Detail · Program Detail) | `exercise/` |
| B4 | Assessment / AI (Flow · Results · AI Review) | `assessment/` |
| B5 | Body Awareness (Compensation Profile · Goals List · Goal Detail) | `body-awareness/` |
| B6 | Daily Logging (Journal · Nutrition · Sleep) | `daily-logging/` |
| B7 | Profile | `profile/` |

Each group produces a `spec.md` + `test-cases.md`. **DoD requires ≥3 edge cases the spec author had to dig out of the actual code** — paraphrasing `plan.md` is explicitly called out as adding no value.

The worktree branches off `main` HEAD. By the time you spawn it, that HEAD will have the merged commits above, so the worktree will see all the C1/C2/D code and the synced docs — exactly what each spec author needs to read.

**Suggested dispatch:** one worktree agent at a time per group (the docs need consistency in voice and structure across the 7 specs), or one umbrella agent that produces all 7 sequentially. Don't fan out 7 worktrees in parallel — they'd produce inconsistent specs and clash on the `docs/screens/revamp-v1/` shared folder structure.

## Working tree state at handoff (2026-04-27)

```
Modified:
  docs/branding/brand-identity-plan.md                                       (unrelated WIP)
  docs/branding/logo/logo-stacked.svg                                        (unrelated WIP)
  docs/branding/preview.html                                                 (unrelated WIP)
  docs/phases/phase01-tasks.md                                               (Phase A edit ✅)
  docs/screens/revamp-v1/design/mockups.html                                 (uninspected)
  docs/screens/revamp-v1/discovery/plan.md                                   (Phase A edit ✅)
  frontend/mobile/lib/core/splash/splash_page.dart                           (unrelated WIP)
  frontend/mobile/lib/core/splash/splash_page_test.dart                      (unrelated WIP)
  frontend/mobile/lib/features/auth/presentation/pages/sign_up_page.dart     (unrelated WIP)
  frontend/mobile/lib/features/dashboard/presentation/pages/home_page.dart                  (Phase C1)
  frontend/mobile/lib/features/dashboard/presentation/pages/home_page_test.dart             (Phase C1)
  frontend/mobile/lib/features/journal/presentation/pages/journal_entry_page_test.dart      (Phase D)
  frontend/mobile/lib/features/journal/presentation/widgets/voice_input_widget.dart         (Phase D)
  frontend/mobile/lib/features/profile/presentation/pages/onboarding_flow.dart              (Phase C2)
  frontend/mobile/lib/features/profile/presentation/pages/onboarding_flow_test.dart         (Phase C2)

Untracked:
  docs/screens/revamp-v1/discovery/handoff-2026-04-26.md                     (NEW, prior session)
  docs/screens/revamp-v1/discovery/handoff-2026-04-27.md                     (NEW, this file)
  docs/screens/revamp-v1/discovery/phases-plan.md                            (NEW; Phase A.1 edits still pending)
  docs/screens/revamp-v1/discovery/sonnet-handoff-phases-C-D.md              (NEW, prior session)
  frontend/mobile/lib/features/dashboard/presentation/pages/today_focal_card_test.dart      (Phase C1)
  frontend/web-recorder/                                                     (sibling feature, see memory)
```

No commits since `1ab20cb feat(backend): cross-app assistant ingest contract`.

## Suggested next-step priority

1. **Finish Phase A** — apply the three marker edits to `phases-plan.md` (~3 min).
2. **Inspect uninspected diffs** — `git diff docs/screens/revamp-v1/design/mockups.html docs/branding/ frontend/mobile/lib/core/splash/ frontend/mobile/lib/features/auth/presentation/pages/sign_up_page.dart` so commits #5 and #6 get accurate subjects (~5 min).
3. **Commit in 7 logical groups** as above (~10 min).
4. **Push** with hooks (~2 min + test runtime).
5. **Spawn worktree agent for Phase B** (one group at a time or one umbrella agent — see §4 above).

Total to clear `main` and dispatch B: ~20 min plus pre-push test runtime.
