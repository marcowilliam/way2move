# STATUS — Pointer

The canonical STATUS.md for way2move lives in the CNS:

**`/projects/my-projects/marco-cns/projects/way2move/STATUS.md`** *(created 2026-04-21)*

Do not add content here — update the CNS version instead.

### Why it lives in CNS
Status changes frequently and is a cross-project concern (the CNS tracks status for all of Marco's projects in one place). Keeping the canonical copy in CNS means any tool that reads project state has one address to query.

### Related docs that stay in this repo
- `docs/FEATURES.md` — feature catalog (what each feature is + how to test in UI)
- `docs/RELEASE_PLAN.md` — release-ship decisions + flag targets
- `docs/phases/phase0X-tasks.md` — authoritative per-block task status (source of truth for completion %)
- `docs/FEATURE_FLAGS_INVENTORY.md` — flag catalog
